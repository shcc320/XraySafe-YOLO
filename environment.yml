name: xraysafe-yolo
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch
  - torchvision
  - pytorch-cuda=12.1
  - pip:
      - ultralytics>=8.3.0
      - opencv-python>=4.8.0
      - pillow>=10.0.0
      - pandas>=2.0.0
      - pyyaml>=6.0.0
      - tqdm>=4.66.0
      - matplotlib>=3.8.0
      - numpy>=1.24.0
      - lxml>=4.9.0
      - tabulate>=0.9.0
      - jinja2>=3.1.0
